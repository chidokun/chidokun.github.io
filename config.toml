# Tranquilpeak
# Version : 0.5.0-BETA
# Author : Thibaud Leprêtre

# I STRONGLY recommend you to use a CDN to speed up loading of pages.
# There is many free CDN like Cloudinary or you can also use indirectly
# by using services like Google Photos.

# If you want to store images yourself, please read this guidelines:
# For users, if you only use this theme, put your images directly in `source/assets/images` folder
# But if you want to add local images, you can put your images directly in `source/assets/images` folder
# For developpers, if you modify this theme, put your images in `source/images` folder and
# use grunt task `build` to synchronize assets

baseURL = "https://chidokun.github.io/"
languageCode = "vi"
defaultContentLanguage = "vi"
title = "Nguyễn Tuấn's Blog"
theme = "tranquilpeak"
# disqusShortname = "nguyentuanit96"
googleAnalytics = "UA-64965138-2"
paginate = 6
canonifyurls = true
enableRobotsTXT = true
enableEmoji = true

[permalinks]
  posts = "/:year/:month/:slug/"

[taxonomies]
  tag = "tags"
  category = "categories"
  archive = "archives"

# ------
# Markup
# ------
[markup]
  [markup.highlight]
    # Disable Chroma server side syntax highlighter in code fences
    codeFences = false
  [markup.goldmark.renderer]
    # Render raw HTML Tag
    unsafe = true 
  # Set startLevel = 1 to support # title (<h1>title</h1>) otherwise table of content is blank/empty
  [markup.tableOfContents]
    endLevel = 3
    ordered = false
    startLevel = 1

# ------
# Author
# ------
[author]
  name = "Nguyễn Tuấn"
  bio = "A guy with passionate at the code"
  job = "Software Engineer"
  location = "Saigon, Vietnam"
  # Your Gravatar email. Overwrite `author.picture` everywhere in the blog
  gravatarEmail = "nguyentuanit96@gmail.com"
  # Your profile picture
  # Overwritten by your gravatar image if `author.gravatarEmail` is filled
  # picture = "https://cdn1.iconfinder.com/data/icons/ninja-things-1/1772/ninja-simple-512.png"
  # Your Twitter username without the @. E.g : thibaudlepretre
  # twitter = "thibaudlepretre"
  # Your google plus profile id. E.g : +ThibaudLepretre or 114625208755123718311
  # googlePlus = "+ThibaudLepretre"

# ------------
# Sidebar menu
# ------------
[[menu.main]]
  weight = 1
  identifier = "home"
  name = "Home"
  pre = "<i class=\"sidebar-button-icon fas fa-lg fa-home\" aria-hidden=\"true\"></i>"
  url = "/"
[[menu.main]]
  weight = 2
  identifier = "software"
  name = "Lập trình"
  pre = "<i class=\"sidebar-button-icon far fa-lg fa-code\" aria-hidden=\"true\"></i>"
  url = "/categories/lập-trình/"
[[menu.main]]
  weight = 3
  identifier = "data"
  name = "Dữ liệu"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-database\" aria-hidden=\"true\"></i>"
  url = "/categories/dữ-liệu/"
[[menu.main]]
  weight = 4
  identifier = "life"
  name = "Cuộc sống"
  pre = "<i class=\"sidebar-button-icon fab fa-lg fa-pagelines\" aria-hidden=\"true\"></i>"
  url = "/categories/cuộc-sống/"
[[menu.main]]
  weight = 5
  identifier = "categories"
  name = "Categories"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-bookmark\" aria-hidden=\"true\"></i>"
  url = "/categories"
[[menu.main]]
  weight = 6
  identifier = "tags"
  name = "Tags"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-tags\" aria-hidden=\"true\"></i>"
  url = "/tags"
[[menu.main]]
  weight = 7
  identifier = "archives"
  name = "Archives"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-archive\" aria-hidden=\"true\"></i>"
  url = "/archives"
[[menu.main]]
  weight = 8
  identifier = "about"
  name = "About"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-question\" aria-hidden=\"true\"></i>"
  url = "/#about"

[[menu.links]]
  weight = 1
  identifier = "github"
  name = "GitHub"
  pre = "<i class=\"sidebar-button-icon fab fa-lg fa-github\" aria-hidden=\"true\"></i>"
  url = "https://github.com/chidokun"

[[menu.misc]]
  weight = 1
  identifier = "rss"
  name = "RSS"
  pre = "<i class=\"sidebar-button-icon fa fa-lg fa-rss\"></i>"
  url = "/index.xml"

# -------------
# Customization
# -------------
[params]
  # -----------
  # Description
  # -----------
  description = "Nguyễn Tuấn's Blog"

  # ------------------
  # Syntax highlighter
  # ------------------
  # Syntax highlighter, possible choice between: "highlight.js" and "prism.js"
  # You can comment it to disable syntax highlighting
  syntaxHighlighter = "highlight.js"

  # -----------
  # Date format
  # -----------
  # Customize date format use to render blog post date, categories and other
  # You must use date format used by Go Time package https://golang.org/pkg/time/
  # Months (not work with short month like "jan", "feb", etc) are translated if translation exists on i18n folders
  # Default format is: January 2, 2006
  dateFormat = "2 January 2006"

  # Global keywords configuration. Following keywords will be add to every pages
  keywords = ["development", "programming", "chidokun", "lập trình"]

  # Hide sidebar on all article page to let article take full width to improve reading, and enjoy wide images and cover images. (true: enable, false: disable)
  clearReading = true

  # Define categories will create hierarchy between parents: `categories = ["foo", "bar"]` will consider "bar" a sub-category of "foo".
  # If false it will flat categories.
  hierarchicalCategories = true

  # Google Site Verification
  googleSiteVerfication = "MgXdDXZwDhcwwTzczA6YhQBynidGFAQUBd7PKu2ubfI"

  # Display `Next` on left side of the pagination, and `Prev` on right side one.
  # If you set this value to `true`, these positions swap.
  #  swapPaginator = true

  # ---------------------
  # Sidebar configuration
  # ---------------------
  # Customization
  # Define the behavior of the sidebar
  # 1: Display extra large sidebar on extra large screen, large sidebar on large screen,
  #    medium sidebar on medium screen and header bar on small screen and
  # extra large sidebar is swiped on extra large screen and large sidebar on all lower screen (default)
  # 2: Display large sidebar on large screen, medium sidebar on medium screen and
  #    header bar on small screen and large sidebar is swiped
  # 3: Display medium sidebar on large and medium screen and header bar on small screen and
  #    medium sidebar is swiped
  # 4: Display header bar on all screens, extra large sidebar is swiped on extra large screen and
  #    large sidebar is swiped on all lower screens
  # 5: Display header bar on all screens and large sidebar is swiped on large screen
  # 6: Display header bar on all screens and medium sidebar is swiped
  sidebarBehavior = 2

  # Customize link of author avatar in sidebar
  #  [params.sidebar.profile]
  #    url = "/#about"

  # -------------------
  # Media configuration
  # -------------------
  # Your blog cover picture. I STRONGLY recommend you to use a CDN to speed up loading of pages.
  # There is many free CDN like Cloudinary or you can also use indirectly
  # by using services like Google Photos.
  # Current image is on AWS S3 and delivered by AWS CloudFront.
  # Otherwise put your image, for example `cover.jpg` in folder `static/images/`,
  # and use relative url : `images/cover.jpg`
  coverImage = "images/background.jpg"

  # Display an image gallery at the end of a post which have photos variables (false: disabled, true: enabled)
  imageGallery = true

  # Display thumbnail image of each post on index pages (false: disabled, true: enabled)
  thumbnailImage = true
  # Display thumbnail image at the right of title in index pages (`right`, `left` or `bottom`)
  # Set this value to `right` if you have old posts to keep the old style on them
  # and define `thumbnailImagePosition` on a post to overwrite this setting
  thumbnailImagePosition = "bottom"
  # Automatically select the cover image or the first photo from the gallery of a post if there is no thumbnail image as the thumbnail image
  # Set this value to `true` if you have old posts that use the cover image or the first photo as the thumbnail image
  # and set `autoThumbnailImage` to `false` on a post to overwrite this setting
  autoThumbnailImage = true

  # Your favicon path, default is "/favicon.png"
  # favicon = "/favicon.png"

  # ------------------
  # External resources
  # ------------------
  # Custom CSS. Put here your custom CSS files. They are loaded after the theme CSS;
  # they have to be referred from static root. Example
  #  [[params.customCSS]]
  #    href = "styles/styles.css"
  [[params.customCSS]]
    href = "https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.9.0/katex.min.css"
    integrity = "sha384-TEMocfGvRuD1rIAacqrknm5BQZ7W7uWitoih+jMNFXQIbNl16bO8OZmylH/Vi/Ei"
    crossorigin = "anonymous"
  [[params.customCSS]]
    href = "styles/styles.css"

  # Custom JS. Put here your custom JS files. They are loaded after the theme JS;
  # they have to be referred from static root. Example
  #  [[params.customJS]]
  #     src = "js/myscript.js"
  [[params.customJS]]
    src = "https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.1.0/languages/yaml.min.js"
    integrity = "sha512-28oDiQZGKUVN6wQ7PSLPNipOcmkCALXKwOi7bnkyFf8QiMZQxG9EQoy/iiNx6Zxj2cG2SbVa4dXKigQhu7GiFw=="
    crossorigin = "anonymous"
    async = true
    defer = true
  [[params.customJS]]
    src = "https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.9.0/katex.min.js"
    integrity = "sha384-jmxIlussZWB7qCuB+PgKG1uLjjxbVVIayPJwi6cG6Zb4YKq0JIw+OMnkkEC7kYCq"
    crossorigin = "anonymous"
  [[params.customJS]]
    src = "https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.9.0/contrib/auto-render.min.js"
    integrity = "sha384-IiI65aU9ZYub2MY9zhtKd1H2ps7xxf+eb2YFG9lX6uRqpXCvBTOidPRCXCrQ++Uc"
    crossorigin = "anonymous"
  [[params.customJS]]
    src = "js/main.js"
  
  # ---------------
  # Sharing options
  # ---------------
  # Comment and uncomment to enable or disable sharing options
  # If you wanna add a sharing option, read user documentation :
  # Tranquilpeak configuration > Theme configuration > sharing-options
  [[params.sharingOptions]]
    name = "Facebook"
    icon = "fab fa-facebook"
    url = "https://www.facebook.com/sharer/sharer.php?u=%s"

  [[params.sharingOptions]]
    name = "Twitter"
    icon = "fab fa-twitter"
    url = "https://twitter.com/intent/tweet?text=%s"

  # -------------------
  # Integrated services
  # -------------------
  # Gravatar
  #  >> See Author section
  #    [author]
  #      gravatarEmail =
  # Google Analytics
  #  >> See googleAnalytics
  #    googleAnalytics =
  # Your Facebook user ids used to connect your blog with your facebook user accounts (Facebook Insights). Separate ids with comma. e.g : 9830047,1003342
   fbAdminIds = ""
  # Your Facebook app id used to connect your blog with your facebook app account (Facebook Insights). E.g : 9841307
   fbAppId = "445006706177461"

  # --------------
  # Comment system
  # --------------
  [params.comment]
    [params.comment.disqus]
      enable = false
      shortname = "hugo-tranquilpeak-theme"
    [params.comment.gitalk]
      enable = false
      # clientId =
      # clientSecret =
      # owner =
      # repo =
      # See all options: https://github.com/gitalk/gitalk#options
      [params.comment.gitalk.options]
        language = "en"
        perPage = 10
        distractionFreeMode = false
        enableHotKey = true
        pagerDirection = "first"

  # --------------------
  # Header configuration
  # --------------------
  # The link at the right of the header is customizable
  # You can add a link (as an icon) at the right of the header instead of the author's gravatar image or author's picture.
  # By default, author's gravatar or author's picture is displayed.
  #  url: /#search
  #  icon: search
  #  class: st-search-show-outputs
  [params.header.rightLink]
    class = "st-search-show-outputs"
    icon = "search"
    url = "/#search"

  # --------------------
  # Footer configuration
  # --------------------
  # Customize copyright value "© 2017 <CUSTOMIZATION>. All Rights Reserved"
  [params.footer]
    copyright = "<a href=\"https://github.com/chidokun\">Nguyễn Tuấn</a>"

  [params.staticman]
    endpoint = "https://chidokun-blog-comment.herokuapp.com/v3/entry/github"
    username = "chidokun"
    repository = "chidokun.github.io"
    branch = "master"